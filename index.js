var CafeDeli = [];

function takeANumber(CafeDeli,name) {
 CafeDeli.push(name);
return `Welcome, ${name}. You are number ${CafeDeli.length} in line.`;
}

function nowServing(CafeDeli) {
  let i = 1 
  if (cafeDeli.length >= 1) {
    return
  } 
}